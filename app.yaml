command:
  - "python"
  - "app.py"

env:
  # Foundation Model endpoint for AI-powered rule suggestions
  - name: SERVING_ENDPOINT
    value: databricks-claude-sonnet-4-5

resources:
  - name: dqx-db
    database:
      instance_name: dqx-lakebase
      database_name: dqx_rules
      permission: CAN_CONNECT_AND_CREATE
